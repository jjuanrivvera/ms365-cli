# .goreleaser.yaml — ms365 release pipeline (cliwright GOAL.md §5).

version: 2

before:
  hooks: [go mod tidy, ./scripts/completions.sh]

builds:
  - id: ms365
    main: ./cmd/ms365
    binary: ms365
    env: [CGO_ENABLED=0]
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - { goos: windows, goarch: arm64 }
    ldflags:
      - -s -w
      - -X {{ .ModulePath }}/internal/version.Version={{ .Version }}
      - -X {{ .ModulePath }}/internal/version.Commit={{ .Commit }}
      - -X {{ .ModulePath }}/internal/version.Date={{ .Date }}

archives:
  - formats: [tar.gz]
    format_overrides:
      - { goos: windows, formats: [zip] }
    files: [README.md, LICENSE, completions/*]

checksum: { name_template: checksums.txt }

# Docker: a tiny distroless image built from the prebuilt static amd64 binary.
# CI snapshot runs pass --skip=docker (no registry auth on PRs); the real image is built and
# pushed to GHCR at tag time (the release workflow logs in to ghcr.io first).
dockers:
  - id: ms365
    ids: [ms365]
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile.goreleaser
    image_templates:
      - "ghcr.io/jjuanrivvera/ms365-cli:{{ .Version }}"
      - "ghcr.io/jjuanrivvera/ms365-cli:v{{ .Major }}"
      - "ghcr.io/jjuanrivvera/ms365-cli:latest"
    build_flag_templates:
      - "--label=org.opencontainers.image.title=ms365"
      - "--label=org.opencontainers.image.description=A fast, scriptable CLI for Microsoft 365 via Microsoft Graph"
      - "--label=org.opencontainers.image.source=https://github.com/jjuanrivvera/ms365-cli"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.licenses=MIT"

# An explicit release block — without it, `goreleaser check` errors on a fresh repo
# ("scm releases: no remote configured to list refs from").
release:
  github: { owner: jjuanrivvera, name: ms365-cli }
  prerelease: auto

# Homebrew: use homebrew_casks (NOT the deprecated brews:). The cask is ms365-cli,
# but the binary inside the archive is ms365 — so `binaries:` (plural list) is required,
# and an unsigned arm64 binary is SIGKILL'd by Gatekeeper without the xattr postflight.
homebrew_casks:
  - name: ms365-cli
    binaries: [ms365]
    # token required to push the cask cross-repo: the default GITHUB_TOKEN is scoped to this
    # repo only and cannot write to the tap.
    repository: { owner: jjuanrivvera, name: homebrew-ms365-cli, token: "{{ .Env.HOMEBREW_TAP_TOKEN }}" }
    homepage: https://github.com/jjuanrivvera/ms365-cli
    hooks:
      post:
        install: |
          system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/ms365"]

scoops:
  - repository: { owner: jjuanrivvera, name: scoop-ms365-cli, token: "{{ .Env.HOMEBREW_TAP_TOKEN }}" }   # bucket repo must already exist; cross-repo token

nfpms:
  - formats: [deb, rpm, apk]
    bindir: /usr/bin
    maintainer: jjuanrivvera
    contents:
      - { src: completions/ms365.bash, dst: /usr/share/bash-completion/completions/ms365 }
      - { src: completions/ms365.zsh,  dst: /usr/share/zsh/site-functions/_ms365 }
      - { src: completions/ms365.fish, dst: /usr/share/fish/vendor_completions.d/ms365.fish }

sboms:
  - artifacts: archive          # syft SBOMs

signs:                          # cosign keyless (needs CI OIDC: permissions id-token: write)
  - cmd: cosign
    artifacts: checksum
    output: true
    args: ["sign-blob", "--yes", "--output-signature=${signature}", "--output-certificate=${certificate}", "${artifact}"]

changelog:
  groups:
    - { title: Features, regexp: '^.*?feat(\(.+\))??!?:.+$', order: 0 }
    - { title: Fixes, regexp: '^.*?fix(\(.+\))??!?:.+$', order: 1 }
    - { title: Performance, regexp: '^.*?perf(\(.+\))??!?:.+$', order: 2 }
  filters:
    exclude: ['^docs:', '^test:', '^chore:', '^ci:', merge conflict, Merge pull request, Merge branch]
